package org.apache.kafka.streams;

import org.apache.kafka.streams.kstream.KStream;

/**
 * Extension of KStreams that adds retryable methods.
 */
public interface RetryableKStream extends KStream {
}
